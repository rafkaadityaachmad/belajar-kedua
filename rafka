<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio Galaksi Rafka | TKJ Gamer</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', 'Tahoma', Geneva, Verdana, sans-serif;
            background-color: #050510;
            color: #fff;
            overflow-x: hidden;
            line-height: 1.6;
            min-height: 100vh;
            position: relative;
        }

        /* Background galaksi */
        .galaxy-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: 
                radial-gradient(circle at 10% 20%, rgba(30, 10, 80, 0.8) 0%, transparent 40%),
                radial-gradient(circle at 90% 30%, rgba(100, 20, 120, 0.6) 0%, transparent 40%),
                radial-gradient(circle at 50% 80%, rgba(10, 30, 100, 0.7) 0%, transparent 40%),
                linear-gradient(180deg, #050510 0%, #0a0a20 30%, #151530 100%);
        }

        /* Bintang-bintang berkelap-kelip */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .star {
            position: absolute;
            background-color: white;
            border-radius: 50%;
            animation: twinkle 3s infinite alternate;
        }

        @keyframes twinkle {
            0% { opacity: 0.3; }
            100% { opacity: 1; }
        }

        /* Planet berputar */
        .planet {
            position: fixed;
            border-radius: 50%;
            z-index: -1;
            opacity: 0.6;
            filter: blur(1px);
        }

        .planet-1 {
            width: 300px;
            height: 300px;
            top: 10%;
            right: 5%;
            background: radial-gradient(circle at 30% 30%, #a855f7, #3b0764);
            animation: rotatePlanet 40s linear infinite;
        }

        .planet-2 {
            width: 200px;
            height: 200px;
            bottom: 15%;
            left: 10%;
            background: radial-gradient(circle at 40% 40%, #0ea5e9, #1e40af);
            animation: rotatePlanet 30s linear infinite reverse;
        }

        @keyframes rotatePlanet {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Gaming-style cursor */
        body {
            cursor: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><circle cx="12" cy="12" r="8" fill="%23a855f7" opacity="0.9"/><circle cx="12" cy="12" r="4" fill="%230ea5e9" opacity="0.9"/></svg>') 12 12, auto;
        }

        /* Header dengan galaksi */
        header {
            background: rgba(10, 15, 40, 0.7);
            padding: 80px 20px;
            text-align: center;
            position: relative;
            border-bottom: 2px solid #0ea5e9;
            backdrop-filter: blur(10px);
            margin-bottom: 60px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }

        .header-content {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
            z-index: 2;
        }

        .galaxy-text {
            color: #fff;
            text-shadow: 
                0 0 10px #0ea5e9,
                0 0 20px #0ea5e9,
                0 0 30px #0ea5e9,
                0 0 40px #a855f7,
                0 0 70px #a855f7,
                0 0 80px #a855f7;
            animation: galaxyPulse 2s infinite alternate;
        }

        @keyframes galaxyPulse {
            0% { 
                text-shadow: 
                    0 0 10px #0ea5e9,
                    0 0 20px #0ea5e9,
                    0 0 30px #0ea5e9,
                    0 0 40px #a855f7,
                    0 0 70px #a855f7;
            }
            100% { 
                text-shadow: 
                    0 0 15px #0ea5e9,
                    0 0 25px #0ea5e9,
                    0 0 35px #0ea5e9,
                    0 0 50px #a855f7,
                    0 0 90px #a855f7,
                    0 0 100px #a855f7;
            }
        }

        header h1 {
            font-size: 52px;
            margin-bottom: 15px;
            letter-spacing: 3px;
        }

        .gamer-tag {
            display: inline-block;
            background: linear-gradient(90deg, #0ea5e9, #a855f7);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-weight: bold;
            padding: 10px 25px;
            border: 2px solid #0ea5e9;
            border-radius: 30px;
            margin-bottom: 20px;
            font-size: 20px;
            letter-spacing: 1px;
            animation: borderFlow 3s infinite linear;
            position: relative;
            overflow: hidden;
        }

        .gamer-tag::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            animation: tagShine 3s infinite;
        }

        @keyframes borderFlow {
            0% { border-color: #0ea5e9; }
            50% { border-color: #a855f7; }
            100% { border-color: #0ea5e9; }
        }

        @keyframes tagShine {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        .subtitle {
            font-size: 22px;
            color: #bae6fd;
            margin-bottom: 30px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            text-shadow: 0 0 10px rgba(14, 165, 233, 0.5);
        }

        /* Stats Gaming */
        .stats-container {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 50px;
            flex-wrap: wrap;
        }

        .stat-card {
            background: rgba(20, 25, 60, 0.7);
            border-radius: 15px;
            padding: 25px;
            width: 200px;
            text-align: center;
            border: 1px solid rgba(14, 165, 233, 0.4);
            transition: all 0.4s ease;
            transform-style: preserve-3d;
            perspective: 1000px;
            backdrop-filter: blur(5px);
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .stat-card::after {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, #0ea5e9, #a855f7, #0ea5e9);
            border-radius: 17px;
            z-index: -1;
            opacity: 0;
            transition: opacity 0.4s;
        }

        .stat-card:hover::after {
            opacity: 1;
        }

        .stat-card:active {
            transform: scale(0.95) translateY(-5px);
            box-shadow: 0 5px 20px rgba(168, 85, 247, 0.6);
        }

        .stat-card:hover {
            transform: translateY(-10px) rotateX(10deg);
            box-shadow: 0 15px 30px rgba(14, 165, 233, 0.5);
            border-color: #a855f7;
        }

        .stat-icon {
            font-size: 40px;
            margin-bottom: 15px;
            color: #0ea5e9;
            text-shadow: 0 0 15px rgba(14, 165, 233, 0.7);
        }

        .stat-value {
            font-size: 32px;
            font-weight: bold;
            color: #a855f7;
            margin-bottom: 5px;
            text-shadow: 0 0 10px rgba(168, 85, 247, 0.7);
        }

        .stat-label {
            font-size: 16px;
            color: #bae6fd;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Main content */
        section {
            max-width: 1100px;
            margin: 60px auto;
            padding: 0 20px;
        }

        .section-title {
            font-size: 36px;
            margin-bottom: 40px;
            position: relative;
            display: inline-block;
            padding-left: 20px;
            background: linear-gradient(90deg, #0ea5e9, #a855f7);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .section-title::before {
            content: '✦';
            position: absolute;
            left: -10px;
            color: #0ea5e9;
            font-size: 40px;
            animation: spin 5s infinite linear;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .about, .projects {
            background: rgba(20, 25, 60, 0.7);
            backdrop-filter: blur(15px);
            padding: 50px;
            border-radius: 20px;
            border: 1px solid rgba(14, 165, 233, 0.3);
            margin-bottom: 60px;
            position: relative;
            overflow: hidden;
            transition: all 0.4s ease;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
        }

        .about::before, .projects::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(14, 165, 233, 0.1) 0%, rgba(168, 85, 247, 0.1) 100%);
            z-index: -1;
        }

        .about:hover, .projects:hover {
            box-shadow: 0 20px 40px rgba(14, 165, 233, 0.4);
            border-color: #a855f7;
            transform: translateY(-5px);
        }

        .skills {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 30px;
        }

        .skill-tag {
            background: rgba(14, 165, 233, 0.15);
            border: 1px solid rgba(14, 165, 233, 0.5);
            padding: 12px 24px;
            border-radius: 30px;
            font-size: 16px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .skill-tag:hover {
            transform: scale(1.1);
            box-shadow: 0 0 20px rgba(14, 165, 233, 0.7);
            background: rgba(168, 85, 247, 0.2);
            border-color: #a855f7;
        }

        .skill-tag:active {
            transform: scale(0.95);
            box-shadow: 0 0 15px rgba(168, 85, 247, 0.9);
        }

        .skill-tag::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: all 0.6s;
        }

        .skill-tag:hover::before {
            left: 100%;
        }

        /* Project items dengan animasi galaksi */
        .project-item {
            background: rgba(25, 30, 70, 0.8);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            border-left: 5px solid #0ea5e9;
            transition: all 0.5s ease;
            position: relative;
            overflow: hidden;
            cursor: pointer;
            backdrop-filter: blur(5px);
        }

        .project-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, transparent 0%, rgba(14, 165, 233, 0.1) 100%);
            z-index: -1;
        }

        .project-item:hover::before {
            background: linear-gradient(135deg, transparent 0%, rgba(168, 85, 247, 0.1) 100%);
        }

        .project-item:active {
            transform: scale(0.98) translateX(5px);
            box-shadow: 0 5px 15px rgba(168, 85, 247, 0.6);
        }

        .project-item:hover {
            transform: translateX(15px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
            border-left-color: #a855f7;
        }

        .project-item h3 {
            color: #0ea5e9;
            margin-bottom: 20px;
            font-size: 24px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .project-icon {
            font-size: 22px;
            text-shadow: 0 0 10px rgba(14, 165, 233, 0.7);
        }

        .project-item p {
            color: #dbeafe;
            line-height: 1.8;
            font-size: 17px;
        }

        /* Progress bar untuk project difficulty */
        .difficulty {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-top: 20px;
        }

        .difficulty-label {
            font-size: 16px;
            color: #bae6fd;
            min-width: 140px;
        }

        .progress-bar {
            height: 12px;
            background: rgba(14, 165, 233, 0.2);
            border-radius: 6px;
            flex-grow: 1;
            overflow: hidden;
            border: 1px solid rgba(14, 165, 233, 0.3);
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #0ea5e9, #a855f7);
            border-radius: 6px;
            animation: progressFill 1.5s ease-out;
            position: relative;
            overflow: hidden;
        }

        .progress-fill::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            animation: progressShine 2s infinite;
        }

        @keyframes progressFill {
            0% { width: 0%; }
            100% { width: var(--progress); }
        }

        @keyframes progressShine {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 60px 20px;
            background: rgba(5, 10, 40, 0.9);
            border-top: 2px solid #0ea5e9;
            position: relative;
            margin-top: 100px;
            backdrop-filter: blur(10px);
            box-shadow: 0 -10px 30px rgba(0, 0, 0, 0.3);
        }

        .social-icons {
            display: flex;
            justify-content: center;
            gap: 25px;
            margin: 40px 0;
        }

        .social-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: rgba(14, 165, 233, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 24px;
            transition: all 0.4s ease;
            border: 2px solid rgba(14, 165, 233, 0.4);
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .social-icon::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(14, 165, 233, 0.3), rgba(168, 85, 247, 0.3));
            border-radius: 50%;
            transform: scale(0);
            transition: all 0.4s ease;
        }

        .social-icon:hover::before {
            transform: scale(1);
        }

        .social-icon:hover {
            transform: translateY(-10px) scale(1.1);
            box-shadow: 0 10px 25px rgba(14, 165, 233, 0.5);
            border-color: #a855f7;
        }

        .social-icon:active {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 5px 15px rgba(168, 85, 247, 0.7);
        }

        .social-icon i {
            position: relative;
            z-index: 1;
        }

        .copyright {
            color: #bae6fd;
            font-size: 16px;
            margin-top: 30px;
            text-shadow: 0 0 10px rgba(14, 165, 233, 0.5);
        }

        /* Komet animasi */
        .comet {
            position: fixed;
            width: 4px;
            height: 4px;
            background: white;
            border-radius: 50%;
            box-shadow: 0 0 10px 2px #0ea5e9;
            z-index: -1;
            opacity: 0;
        }

        /* Animasi masuk */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .header-content, .about, .project-item {
            animation: fadeInUp 1s ease-out forwards;
        }

        .project-item:nth-child(1) { animation-delay: 0.1s; }
        .project-item:nth-child(2) { animation-delay: 0.2s; }
        .project-item:nth-child(3) { animation-delay: 0.3s; }
        .project-item:nth-child(4) { animation-delay: 0.4s; }

        /* Responsive */
        @media (max-width: 768px) {
            header h1 {
                font-size: 36px;
            }
            
            .subtitle {
                font-size: 18px;
                padding: 0 10px;
            }
            
            .stats-container {
                flex-direction: column;
                align-items: center;
            }
            
            .about, .projects {
                padding: 30px 20px;
            }
            
            .section-title {
                font-size: 28px;
            }
            
            .planet-1, .planet-2 {
                display: none;
            }
        }
    </style>
</head>
<body>
    <!-- Background galaksi -->
    <div class="galaxy-bg"></div>
    
    <!-- Bintang-bintang -->
    <div class="stars" id="stars"></div>
    
    <!-- Planet -->
    <div class="planet planet-1"></div>
    <div class="planet planet-2"></div>

    <header>
        <div class="header-content">
            <div class="gamer-tag">TKJ_GAMER_01</div>
            <h1 class="galaxy-text">RAFKA</h1>
            <p class="subtitle">Penjelajah Galaksi Teknologi | Siswa TKJ SMKS 1 Barunawati</p>
            
            <div class="stats-container">
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-rocket"></i>
                    </div>
                    <div class="stat-value">15+</div>
                    <div class="stat-label">Misi Selesai</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-satellite"></i>
                    </div>
                    <div class="stat-value">99.9%</div>
                    <div class="stat-label">Stabilitas Jaringan</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-gamepad"></i>
                    </div>
                    <div class="stat-value">LVL 99</div>
                    <div class="stat-label">Gamer Rank</div>
                </div>
            </div>
        </div>
    </header>

    <section>
        <div class="about">
            <h2 class="section-title">PROFIL ANTARIKSA</h2>
            <p>
                Saya adalah penjelajah galaksi teknologi dari jurusan Teknik Komputer dan Telekomunikasi (TKJ) di SMKS 1 Barunawati. Dengan semangat seperti seorang gamer menaklukkan misi baru, saya menjelajahi bidang jaringan komputer, perakitan perangkat keras, dan pengembangan web. Dalam setiap ekspedisi teknologi, saya membawa prinsip performa maksimal dan koneksi stabil seperti yang dibutuhkan dalam gaming kompetitif.
            </p>
            
            <div class="skills">
                <div class="skill-tag">Jaringan Galaksi</div>
                <div class="skill-tag">Perakitan Stasiun Luar Angkasa</div>
                <div class="skill-tag">Pengembangan Web Antariksa</div>
                <div class="skill-tag">PC Gaming Orbit</div>
                <div class="skill-tag">Pemecahan Masalah Sistem</div>
                <div class="skill-tag">Optimasi Kecepatan Cahaya</div>
            </div>
        </div>

        <div class="projects">
            <h2 class="section-title">MISI YANG SUDAH DILAKUKAN</h2>

            <div class="project-item">
                <h3><i class="fas fa-satellite-dish project-icon"></i> Merakit Stasiun Gaming Orbit</h3>
                <p>Membangun komputer gaming dari komponen hardware terpilih seperti motherboard, processor, RAM, storage, dan power supply untuk performa gaming dan produktivitas tinggi di orbit teknologi.</p>
                <div class="difficulty">
                    <span class="difficulty-label">Tingkat Kesulitan:</span>
                    <div class="progress-bar">
                        <div class="progress-fill" style="--progress: 85%;"></div>
                    </div>
                </div>
            </div>

            <div class="project-item">
                <h3><i class="fas fa-signal project-icon"></i> Instalasi Kabel Jaringan Galaksi</h3>
                <p>Melakukan pemasangan dan crimping kabel LAN (UTP) serta pengujian koneksi jaringan untuk memastikan konektivitas antar planet yang optimal dan stabil.</p>
                <div class="difficulty">
                    <span class="difficulty-label">Tingkat Kesulitan:</span>
                    <div class="progress-bar">
                        <div class="progress-fill" style="--progress: 70%;"></div>
                    </div>
                </div>
            </div>

            <div class="project-item">
                <h3><i class="fas fa-globe-americas project-icon"></i> Memetakan Topologi Jaringan Antariksa</h3>
                <p>Mempelajari dan menerapkan topologi jaringan seperti star, bus, dan ring dalam simulasi untuk memahami arsitektur jaringan galaksi yang efisien.</p>
                <div class="difficulty">
                    <span class="difficulty-label">Tingkat Kesulitan:</span>
                    <div class="progress-bar">
                        <div class="progress-fill" style="--progress: 65%;"></div>
                    </div>
                </div>
            </div>

            <div class="project-item">
                <h3><i class="fas fa-laptop-code project-icon"></i> Membangun Website Portal Antariksa</h3>
                <p>Membuat website dengan HTML dan CSS yang memiliki struktur rapi dan tampilan futuristik responsif di berbagai perangkat antariksa.</p>
                <div class="difficulty">
                    <span class="difficulty-label">Tingkat Kesulitan:</span>
                    <div class="progress-bar">
                        <div class="progress-fill" style="--progress: 60%;"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="social-icons">
            <a href="#" class="social-icon">
                <i class="fab fa-discord"></i>
            </a>
            <a href="#" class="social-icon">
                <i class="fab fa-steam"></i>
            </a>
            <a href="#" class="social-icon">
                <i class="fab fa-github"></i>
            </a>
            <a href="https://www.instagram.com/whos.clbs?igsh=MXI0bWNsdXR3dm83dA==" target="_blank" class="social-icon">
                <i class="fab fa-instagram"></i>
            </a>
        </div>
        <p class="copyright">© 2026 Rafka | Portfolio Galaksi Gaming Edition | SMKS 1 Barunawati</p>
        <p class="copyright">Instagram: <a href="https://www.instagram.com/whos.clbs?igsh=MXI0bWNsdXR3dm83dA==" target="_blank" style="color:#a855f7; text-decoration: none;">@whos.clbs</a></p>
    </footer>

    <script>
        // Animasi bintang
        function createStars() {
            const starsContainer = document.getElementById('stars');
            const starCount = 200;
            
            for (let i = 0; i < starCount; i++) {
                const star = document.createElement('div');
                star.classList.add('star');
                
                // Ukuran acak
                const size = Math.random() * 3 + 1;
                star.style.width = `${size}px`;
                star.style.height = `${size}px`;
                
                // Posisi acak
                star.style.left = `${Math.random() * 100}%`;
                star.style.top = `${Math.random() * 100}%`;
                
                // Animasi kelap-kelip dengan delay acak
                star.style.animationDelay = `${Math.random() * 3}s`;
                
                starsContainer.appendChild(star);
            }
        }
        
        // Animasi komet
        function createComet() {
            const comet = document.createElement('div');
            comet.classList.add('comet');
            
            // Posisi awal acak
            const startX = Math.random() * 100;
            const startY = Math.random() * 30;
            
            comet.style.left = `${startX}%`;
            comet.style.top = `${startY}%`;
            
            // Durasi dan ukuran acak
            const duration = Math.random() * 3 + 2;
            const size = Math.random() * 6 + 2;
            
            comet.style.width = `${size}px`;
            comet.style.height = `${size}px`;
            
            // Warna acak
            const colors = ['#0ea5e9', '#a855f7', '#ffffff'];
            const color = colors[Math.floor(Math.random() * colors.length)];
            comet.style.background = color;
            comet.style.boxShadow = `0 0 10px 2px ${color}`;
            
            document.body.appendChild(comet);
            
            // Animasi komet
            const animation = comet.animate([
                { 
                    opacity: 0,
                    transform: 'translate(0, 0) scale(1)'
                },
                { 
                    opacity: 1,
                    transform: `translate(${Math.random() * 100 - 50}px, ${Math.random() * 100 + 100}px) scale(0.5)`
                },
                { 
                    opacity: 0,
                    transform: `translate(${Math.random() * 200 - 100}px, ${Math.random() * 200 + 200}px) scale(0.1)`
                }
            ], {
                duration: duration * 1000,
                easing: 'cubic-bezier(0.215, 0.610, 0.355, 1)'
            });
            
            // Hapus elemen setelah animasi selesai
            animation.onfinish = () => {
                comet.remove();
            };
        }
        
        // Efek klik
        document.addEventListener('click', function(e) {
            // Efek ripple
            const ripple = document.createElement('div');
            ripple.style.position = 'fixed';
            ripple.style.borderRadius = '50%';
            ripple.style.background = 'radial-gradient(circle, rgba(14,165,233,0.6) 0%, rgba(168,85,247,0.3) 70%, transparent 100%)';
            ripple.style.transform = 'translate(-50%, -50%) scale(0)';
            ripple.style.pointerEvents = 'none';
            ripple.style.zIndex = '9999';
            
            const size = Math.random() * 100 + 50;
            ripple.style.width = `${size}px`;
            ripple.style.height = `${size}px`;
            ripple.style.left = `${e.clientX}px`;
            ripple.style.top = `${e.clientY}px`;
            
            document.body.appendChild(ripple);
            
            // Animasi ripple
            const animation = ripple.animate([
                { transform: 'translate(-50%, -50%) scale(0)', opacity: 1 },
                { transform: 'translate(-50%, -50%) scale(2)', opacity: 0 }
            ], {
                duration: 800,
                easing: 'cubic-bezier(0.215, 0.610, 0.355, 1)'
            });
            
            // Hapus elemen setelah animasi selesai
            animation.onfinish = () => {
                ripple.remove();
            };
            
            // Kadang-kadang buat komet saat klik
            if (Math.random() > 0.7) {
                createComet();
            }
        });
        
        // Efek hover untuk stat cards
        document.addEventListener('DOMContentLoaded', function() {
            createStars();
            
            // Buat komet secara berkala
            setInterval(createComet, 3000);
            
            // Efek hover untuk stat cards
            const statCards = document.querySelectorAll('.stat-card');
            statCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-15px) rotateX(10deg) scale(1.05)';
                });
                
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(-10px) rotateX(5deg)';
                });
            });
            
            // Efek ketik untuk judul
            const title = document.querySelector('header h1');
            const originalText = title.textContent;
            title.textContent = '';
            
            let i = 0;
            function typeWriter() {
                if (i < originalText.length) {
                    title.textContent += originalText.charAt(i);
                    i++;
                    setTimeout(typeWriter, 120);
                }
            }
            
            // Mulai animasi ketik setelah halaman dimuat
            setTimeout(typeWriter, 800);
            
            // Efek scroll untuk project items
            const projectItems = document.querySelectorAll('.project-item');
            const observerOptions = {
                threshold: 0.2,
                rootMargin: '0px 0px -50px 0px'
            };
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateX(0)';
                    }
                });
            }, observerOptions);
            
            projectItems.forEach(item => {
                item.style.opacity = '0';
                item.style.transform = 'translateX(-30px)';
                item.style.transition = 'opacity 0.7s ease, transform 0.7s ease';
                observer.observe(item);
            });
            
            // Tambahkan efek audio klik (opsional)
            const clickSound = new Audio('data:audio/wav;base64,UklGRigAAABXQVZFZm10IBIAAAABAAEARKwAAIhYAQACABAAZGF0YQQAAAAAAA==');
            
            document.addEventListener('click', function() {
                // Hanya mainkan suara jika ada interaksi pengguna terlebih dahulu
                if (window.userInteracted) {
                    clickSound.currentTime = 0;
                    clickSound.play().catch(e => console.log("Audio play failed (normal)"));
                }
            });
            
            // Tandai bahwa pengguna telah berinteraksi
            document.addEventListener('mousedown', function() {
                window.userInteracted = true;
            });
        });
    </script>
</body>
</html>
